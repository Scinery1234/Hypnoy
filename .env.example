# Anthropic API key for the /api/hypnosis script generator.
# Get one at https://platform.claude.com/ (Settings -> API Keys).
# Local dev: copy this file to .env and fill it in, then run `npm run dev:vercel`.
ANTHROPIC_API_KEY=
